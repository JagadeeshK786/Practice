插入头节点和删除头节点必须在同时拿到head锁和头结点的node锁情况下才能进行
删除普通节点必须要同时拿到前一个节点的node锁和将要删除的节点的node锁的情况下才能进行
先用std::lock试试同时锁定。如果这个方法不行，那么手动顺序拿两个锁，两个锁都拿到之后需要判断是否有效

经过测试，确认：
插入的同时，另一线程删除头结点、头结点的下一个节点、其它节点 的操作行为正确
多个线程同时插入的操作行为正确

先开增加线程和清洁线程，然后停止增加，等几秒再开增加线程会炸。

放弃了。。。不做泛型了，就针对IOCP的情况单独做一个，比如用队列（在队列末尾删除超时的）